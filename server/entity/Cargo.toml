[package]
name = "entity"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.152", features = ["derive"] }

[dependencies.sea-orm]
version = "^0.10.6"
default-features = false
features = ["macros", "with-time", "sqlx-postgres", "runtime-tokio-rustls"]
